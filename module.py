# a module is a piece of software that has a specific functionality. 
# each module is a different file, which can be added separately. a test editor is to be used and must be saved 
# in the same folder as the python file
# importing a module using the import statement, but we are intrested in the function in the module
# to import a function or class from a module we use the "from module import" statement
# from square_module import square
# python has it own built-in module that you can import
def square(x):
    result = x*x;
    return result